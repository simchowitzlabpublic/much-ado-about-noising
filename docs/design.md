`losses`: contains loss function for different training objectives. 
`samplers`: contains different integrators for different training objectives.
`networks`: contains different networks. each network will 
`flow_map.py`: wrapper around the network, provide all function interfaces around stochastic interpolant framework. 